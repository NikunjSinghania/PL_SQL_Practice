/*
CREATE TABLE STUDENTS
(
    FNAME VARCHAR(200)
);

CREATE OR REPLACE TRIGGER FIRST_TRIGGER
BEFORE INSERT OR UPDATE OR DELETE ON STUDENTS
FOR EACH ROW
ENABLE
*/
DECLARE
    NAME VARCHAR(200);
BEGIN
    IF INSERTING THEN
        DBMS_OUTPUT.PUT_LINE('INSERTING');
    ELSIF UPDATING THEN
        DBMS_OUTPUT.PUT_LINE('UPDATING');
    ELSIF DELETING THEN
        DBMS_OUTPUT.PUT_LINE('DELETING');
    END IF;
END;
/

INSERT INTO STUDENTS(FNAME)
VALUES('NIKUNJ');

UPDATE STUDENTS SET FNAME = 'ASHU';